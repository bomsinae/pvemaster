# PVE Master 작업 지침

## 개발 규칙

- `PLAN.md`와 `docs/`의 설계를 최우선 기준으로 삼는다.
- 기능을 구현하기 전에 관련 코드를 먼저 읽는다.
- 기존 API 계약을 임의로 변경하지 않는다.
- 변경 범위를 요청된 작업으로 제한한다.
- 비밀값을 코드나 테스트 데이터에 하드코딩하지 않는다.
- Proxmox 토큰과 비밀번호를 로그에 출력하지 않는다.
- 모든 외부 HTTP 요청에 timeout을 설정한다.
- TLS 인증서 검증은 기본으로 활성화하며 `verify=False`를 사용하지 않는다.
- 데이터베이스 변경에는 Alembic migration을 작성한다.
- API 응답에 ORM 객체를 직접 반환하지 않는다.
- 인증과 객체 소유권 검사는 서비스 계층에서 강제한다.
- 프론트엔드 검사만으로 권한을 보호하지 않는다.

## 테스트 규칙

- 단위 테스트에서 실제 Proxmox 서버를 호출하지 않고 mock 또는 fake client를 사용한다.
- 정상 경로와 실패 경로를 모두 테스트한다.
- 인증 실패, 권한 실패, timeout, TLS 오류를 테스트한다.
- 작업 완료 전에 전체 테스트와 Ruff, mypy를 실행한다.

## 작업 완료 보고 형식

1. 변경 내용
2. 변경 파일
3. 실행한 테스트
4. 테스트 결과
5. 남아 있는 위험 또는 후속 작업
